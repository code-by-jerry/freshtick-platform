import { type ReactNode } from 'react';

interface SocietyHomeSectionsProps {
    children: ReactNode;
}

export default function SocietyHomeSections({ children }: SocietyHomeSectionsProps) {
    return <>{children}</>;
}
